{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm run build:packages && pnpm --filter api build",
    "watchPatterns": ["apps/api/src/**"]
  },
  "deploy": {
    "startCommand": "pnpm --filter api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "preDeployCommand": "pnpm install --frozen-lockfile && pnpm run build:packages && pnpm --filter api build && pnpm --filter database exec prisma migrate deploy"
  }
}
